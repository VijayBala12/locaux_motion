import 'package:flutter/material.dart';

class TravelBookScreen extends StatelessWidget {
  const TravelBookScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center( child: Text("Travel book Screen"),),
    );
  }
}
